# Production Environment Variables for Prospect Intelligence Hub
# IMPORTANT: This is a SEPARATE project from mncannabishub
# Do NOT copy mncannabishub environment variables here

# Database - Use SEPARATE production database for this project
DATABASE_URL=postgresql://your-prospect-hub-neon-db-url-here

# NextAuth - MUST be different from mncannabishub
NEXTAUTH_SECRET=GENERATE_NEW_SECRET_WITH_openssl_rand_-base64_32
NEXTAUTH_URL=https://prospect-intelligence-hub-YOUR-DEPLOYMENT.vercel.app

# Apify API (can be same token, but project is separate)
APIFY_API_TOKEN=your-apify-token

# Optional AI Features
GEMINI_API_KEY=your-gemini-key-if-using
ABACUSAI_API_KEY=your-abacus-key-if-using

# Optional AWS S3 (use SEPARATE bucket from mncannabishub)
AWS_BUCKET_NAME=prospect-hub-uploads
AWS_REGION=us-east-1
AWS_FOLDER_PREFIX=prospects
AWS_PROFILE=default

# Optional Gmail OAuth (use SEPARATE OAuth app from mncannabishub)
GOOGLE_CLIENT_ID=your-separate-oauth-client-id
GOOGLE_CLIENT_SECRET=your-separate-oauth-client-secret

# ⚠️ VERIFICATION
# Before deploying, confirm:
# ✅ NEXTAUTH_SECRET is NOT the same as mncannabishub
# ✅ DATABASE_URL points to prospect hub database (NOT mncannabishub database)
# ✅ NEXTAUTH_URL contains "prospect-intelligence-hub" (NOT "mncannabishub")
# ✅ Using separate OAuth app (if using Gmail features)
# ✅ Using separate S3 bucket (if using file uploads)
